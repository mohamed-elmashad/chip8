#include "graphics.h"

Graphics::Graphics() {
    // Initialize screen and SDL library
}

void Graphics::Update() {
    // Render screen using SDL library
}

bool Graphics::DrawSprite(uint8_t x, uint8_t y, uint8_t height, uint8_t* sprite) {
    // Draw a sprite on the screen and check for collision
}
